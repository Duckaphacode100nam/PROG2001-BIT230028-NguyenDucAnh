#include <stdio.h>
#include <stdbool.h>

int main() {
    int age = 20;
    float gpa = 3.5;
    char grade = 'A';
    bool passed = true;

    printf("%d, %.1f, %c, %d\n", age, gpa, grade, passed);

    return 0;
}